filter_images_strings -> for paths like /icons/ and other things that are just noise
hosts -> to further filter trackers and the like

Currently, renderer and renderer-links are identical except for a few changes; if I remember correctly, that's because newsboat doesn't like sending arguments, lol.  They should be mostly done.  Need to document install for helpers and sanity checks if they don't exist.

Wrapper script can call profiles as first argument, and that calls different configs (with the name of the profile at the end) and logs and caches and url lists/sources.

Wrapper script will also handle tmux/kitty;  we need to see if we can pass exported variables from the wrapper script through to the environment that newsboat calls, or if my scripts need to all know how to pick things up.

OH.  One of the profiles wasn't going to send anything to the preview pane, that's right.

in newsbeuter_dangerzone directory
    * "defaults" for ini, config (also no profile runs)
    * executable scripts
        * renderer_links & renderer <- show html
        * filter_images_strings <- skip shitty images
        * hosts <- blocklist

in profile directory:
    * ini file
    * config
    * rest gets populated by newsboat

So basic rendering with the reader profile is working, using env var to toggle links.
Macro for swapping link display.

Added examples using tdab and tdab focus as browser with rdrview and renderer to get sidebar with full text from sites.

WILL USE ENV VARIABLES with out methods

For muttrc:

####################################################
# Renderer Tweaks
####################################################
setenv show_links false # default
setenv clean_links false #default
macro index ,Y "<enter-command>setenv show_links true;echo '[info] Links on; reloading message  '\n<exit><display-message>"
macro pager ,Y "<enter-command>setenv show_links true;echo '[info] Links on; reloading message '\n<exit><display-message>"
macro index ,y "<enter-command>setenv show_links false;echo '[info] Links off; reloading  message'\n<exit><display-message>"
macro pager ,y "<enter-command>setenv show_links false;echo '[info] Links off; reloading  message'\n<exit><display-message>"
macro index ,C "<enter-command>setenv clean_links true;echo '[info] Cleaning on; reloading message (THIS MAY TAKE A MOMENT)'\n<exit><display-message>"
macro pager ,C "<enter-command>setenv clean_links true;echo '[info] Cleaning on; reloading message (THIS MAY TAKE A MOMENT)'\n<exit><display-message>"
macro index ,c "<enter-command>setenv show_links false;echo '[info] Cleaning off; reloading  message'\n<exit><display-message>"
macro pager ,c "<enter-command>setenv show_links false;echo '[info] Cleaning off; reloading  message'\n<exit><display-message>"

# NOTES FOR BOOKMARKER
 also clean links is togggleable.

- calling_program -> agaetr/nbdz (to be initial prompt for where things are)
- if prompt exists or my_CONFIG_DIR
- use calling_program to determine filename of ini, default to agaetr
- that way I'll only have to maintain one between suites.
- also, is there a way to automatically pull muna in? I wonder....

- should I present list of other URLs in the article???


# Also, we're always exporting the links anyway....
add use_kitty switch to renderer and show images and all that.

# TODO - renderer check if from mutt, and if so, we need to process plaintext mails differently.

Have both set env calling_program , additional check about plaintext emails



Next steps:
bookmarker
image display without use_kitty
image display with use_kitty
videos.  use the xml from ytcs.
Syntax: include <path>
Default: n/a
Example: include "~/.newsboat/colors"

With this command, you can include other files to be interpreted as configuration files. This is especially useful to separate your configuration into several files, e.g. key configuration, color configuration, …​
